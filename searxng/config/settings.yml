# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/

use_default_settings: true

server:
  secret_key: "LdfLayBeUpkWQTSExwY3bapuCmVeKIQ"
  image_proxy: true

search:
  formats:
    - html
    - json
